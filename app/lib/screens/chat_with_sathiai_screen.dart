import 'package:flutter/material.dart';

class ChatWithSathiAIScreen extends StatelessWidget {
  const ChatWithSathiAIScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chat with SathiAI')),
      body: Center(child: Text('Chat with SathiAI Screen (UI to be implemented)')),
    );
  }
}

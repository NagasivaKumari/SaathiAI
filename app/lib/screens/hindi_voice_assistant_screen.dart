import 'package:flutter/material.dart';

class HindiVoiceAssistantScreen extends StatelessWidget {
  const HindiVoiceAssistantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hindi Voice Assistant')),
      body: Center(child: Text('Hindi Voice Assistant Screen (UI to be implemented)')),
    );
  }
}

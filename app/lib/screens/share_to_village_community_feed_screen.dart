import 'package:flutter/material.dart';

class ShareToVillageCommunityFeedScreen extends StatelessWidget {
  const ShareToVillageCommunityFeedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Share to Village Community Feed')),
      body: Center(child: Text('UI to be implemented')),
    );
  }
}

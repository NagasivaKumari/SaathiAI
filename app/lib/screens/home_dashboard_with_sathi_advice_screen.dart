import 'package:flutter/material.dart';

class HomeDashboardWithSathiAdviceScreen extends StatelessWidget {
  const HomeDashboardWithSathiAdviceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Home Dashboard With Sathi Advice')),
      body: Center(child: Text('Home Dashboard With Sathi Advice Screen (UI to be implemented)')),
    );
  }
}

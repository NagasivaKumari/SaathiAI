import 'package:flutter/material.dart';

class ActiveSyncingStateScreen2 extends StatelessWidget {
  const ActiveSyncingStateScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sathi Status 2')),
      body: Center(child: Text('Active Syncing State Screen 2 (UI to be implemented)')),
    );
  }
}

import 'package:flutter/material.dart';

class BadgeUnlockedCelebrationWithSoundScreen extends StatelessWidget {
  const BadgeUnlockedCelebrationWithSoundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Badge Unlocked Celebration with Sound')),
      body: Center(child: Text('UI to be implemented')),
    );
  }
}

import 'package:flutter/material.dart';

class BadgeUnlockedCelebrationWithSound2Screen extends StatelessWidget {
  const BadgeUnlockedCelebrationWithSound2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Badge Unlocked Celebration 2')),
      body: Center(child: Text('Badge Unlocked Celebration With Sound 2 Screen (UI to be implemented)')),
    );
  }
}

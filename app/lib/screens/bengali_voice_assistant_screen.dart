import 'package:flutter/material.dart';

class BengaliVoiceAssistantScreen extends StatelessWidget {
  const BengaliVoiceAssistantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bengali Voice Assistant')),
      body: Center(child: Text('Bengali Voice Assistant Screen (UI to be implemented)')),
    );
  }
}
